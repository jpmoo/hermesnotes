import { Sparkles } from "lucide-react";

/** Placeholder for the AI panel (docked at the bottom of the right panel). */
export function AIPanel() {
  return (
    <div className="ai-panel-placeholder">
      <Sparkles size={22} />
      <p>AI assistant coming soon.</p>
    </div>
  );
}
