# The bundle is a build artifact, assembled by build.sh into
# ~/Library/Application Support/Talaria. It is not kept here for two reasons:
# a signature is stale the moment anything inside changes, and this repo lives
# under ~/Documents, which is iCloud-managed — codesign refuses a bundle
# carrying the extended attributes iCloud puts on it, and puts back.
Talaria.app/
